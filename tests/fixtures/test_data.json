{
  "_comment": "Test fixture data - populate with actual IDs from your database",
  "_warning": "Do not commit real IDs to version control if sensitive",

  "testUser": {
    "email": "${TEST_USER_EMAIL}",
    "yachtId": "${TEST_USER_YACHT_ID}",
    "tenantKey": "${TEST_USER_TENANT_KEY}"
  },

  "entities": {
    "_note": "Replace these with actual test entity IDs",
    "workOrderId": "${TEST_WORK_ORDER_ID}",
    "equipmentId": "${TEST_EQUIPMENT_ID}",
    "partId": "${TEST_PART_ID}",
    "faultId": "${TEST_FAULT_ID}"
  },

  "searchQueries": {
    "documents": [
      "generator maintenance procedure",
      "engine overhaul",
      "safety checklist"
    ],
    "equipment": [
      "main engine",
      "generator 1",
      "steering system"
    ]
  },

  "testActions": {
    "updateEquipmentStatus": {
      "validStatuses": ["operational", "degraded", "offline", "maintenance"]
    },
    "addNote": {
      "sampleNotes": [
        "Routine inspection completed",
        "Found minor wear on component",
        "Scheduled for next service"
      ]
    },
    "handover": {
      "categories": ["equipment", "safety", "maintenance", "general"],
      "priorities": ["low", "normal", "high", "critical"]
    }
  }
}
