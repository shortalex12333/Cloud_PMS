# E2E Test Environment Configuration
# ================================================
# Copy this file to .env.e2e.local and fill in actual credentials
# DO NOT COMMIT .env.e2e.local (contains real passwords)

# Frontend URL
PLAYWRIGHT_BASE_URL=https://app.celeste7.ai

# Backend API
RENDER_API_URL=https://pipeline-core.int.celeste7.ai

# Master Supabase (for authentication)
MASTER_SUPABASE_URL=https://qvzmkaamzaqxpzbewjxe.supabase.co
MASTER_SUPABASE_ANON_KEY=<MASTER_ANON_KEY>

# Test Yacht
TEST_USER_YACHT_ID=85fe1119-b04c-41ac-80f1-829d23322598

# Test Accounts (Password2! for all staging users)
# --------------------------------------------------
# CREW - Read-only, no MUTATE actions
CREW_EMAIL=crew.tenant@alex-short.com
CREW_PASSWORD=Password2!

# HOD - Can execute MUTATE actions (receive_part, consume_part)
HOD_EMAIL=hod.tenant@alex-short.com
HOD_PASSWORD=Password2!

# CAPTAIN - Can execute SIGNED actions (write_off_part, adjust_stock_quantity)
CAPTAIN_EMAIL=captain.tenant@alex-short.com
CAPTAIN_PASSWORD=Password2!

# MANAGER - Storage delete permissions
# (Optional - if not available, storage delete tests will be marked pending)
MANAGER_EMAIL=manager.tenant@alex-short.com
MANAGER_PASSWORD=Password2!

# Test Data
# --------------------------------------------------
# Known part ID for testing
TEST_PART_ID=8ad67e2f-2579-4d6c-afd2-0dee85f4d8b3

# Known location ID for receive_part tests
TEST_LOCATION_ID=<VALID_LOCATION_UUID>

# Playwright Options
# --------------------------------------------------
# Set to 'false' to see browser during tests
HEADLESS=true

# Slow down tests for debugging (milliseconds)
SLOWMO=0

# Test timeout (milliseconds)
TEST_TIMEOUT=30000
