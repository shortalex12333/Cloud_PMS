# Legacy Email Components

These components are deprecated. Use the canonical replacements:

| Legacy | Replacement | Notes |
|--------|-------------|-------|
| EmailSearchView.tsx | ../EmailSurface.tsx | Renamed and cleaned; identical logic |

Do not import from _legacy/ in new code.
