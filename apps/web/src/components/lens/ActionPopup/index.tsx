// Re-export only — consumers import from 'ActionPopup', not 'ActionPopup/index'.
// Zero runtime logic here.
export { ActionPopup } from './ActionPopup';
export type { ActionPopupProps, ActionPopupField, ActionPopupGate } from './shared/types';
