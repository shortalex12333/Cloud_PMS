/**
 * CelesteOS Microactions Module
 *
 * Minimal exports - only what's actually used.
 */

// Handler registration (the only externally-used export)
export { registerAllHandlers } from './handlers';
