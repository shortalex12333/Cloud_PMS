# CelesteOS Backend Requirements

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.1
pydantic==2.10.3
slowapi==0.1.9
python-multipart==0.0.9  # Required for file uploads (FastAPI Form/File)

# Supabase Integration
supabase==2.12.0

# PostgreSQL Direct Access (for tenant_pg_gateway)
psycopg2-binary==2.9.10

# JWT Authentication
pyjwt==2.10.1
cryptography==44.0.0

# HTTP Clients
httpx==0.28.1

# JSON Schema Validation
jsonschema==4.23.0

# Environment Variables
python-dotenv==1.0.0

# Caching (TTL cache for tenant lookups)
cachetools==5.3.2

# HTML Parsing (handover export to editable JSON)
beautifulsoup4>=4.12.0

# AI/NLP for extraction pipeline
openai==1.59.5
# spacy is optional - code gracefully degrades without it

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Code Quality
black==24.1.1
flake8==7.0.0
mypy==1.8.0
# sentence-transformers removed - now using OpenAI embeddings
redis>=4.5.0
asyncpg>=0.27.0
