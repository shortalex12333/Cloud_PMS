# Phase 16.1 Plan 01: Summary

**Phase:** 16.1 — Mount /prepare endpoint in pipeline_service
**Plan:** 01
**Completed:** 2026-03-02
**Duration:** ~25 minutes
**Status:** ✅ COMPLETE

---

## One-Liner

Fixed GAP-001 by adding `/prepare` endpoint to `p0_actions_routes.py` — now returns 401 (auth required) instead of 404.

---

## What Was Done

### Code Changes

**File:** `apps/api/routes/p0_actions_routes.py`

1. **Added imports** (lines 51-60):
   ```python
   from action_router.router import (
       PrepareRequest, PrepareResponse, PrefillField,
       AmbiguityCandidate, Ambiguity, PrepareError,
   )
   from common.prefill_engine import build_prepare_response
   ```

2. **Added `/prepare` POST endpoint** (~220 lines):
   - JWT validation with user context extraction
   - Tenant resolution from MASTER DB
   - Yacht isolation validation (RLS enforcement)
   - Domain validation (work_orders, faults, equipment, etc.)
   - Role gating check via ACTION_REGISTRY
   - Calls `build_prepare_response()` from prefill_engine
   - Returns structured `PrepareResponse` with prefill fields, confidence scores, ambiguities

---

## Verification Results

| Check | Result |
|-------|--------|
| Docker build | ✅ SUCCESS |
| Health check | ✅ `{"status":"healthy","version":"1.0.0"}` |
| OpenAPI registration | ✅ `/v1/actions/prepare` present |
| Curl test | ✅ Returns 401 (not 404) |

**Before:** `404 Not Found`
**After:** `{"status":"error","error_code":"missing_token","message":"Authorization token is required"}`

---

## Commits

| Hash | Message |
|------|---------|
| (pending) | fix(16.1-01): mount /prepare endpoint in p0_actions_routes |

---

## Lessons Learned

**Lesson:** FastAPI Router Not Mounted = 404

- Endpoint code existing ≠ endpoint accessible
- Always verify router is mounted in main app (`pipeline_service.py`)
- Don't mount conflicting routers — re-export instead
- Check OpenAPI schema to confirm endpoint registration

**Added to:** `tasks/lessons.md`

---

## Requirements Satisfied

| Requirement | Status |
|-------------|--------|
| GAP-001 | ✅ /prepare endpoint accessible |
| PREFILL-01 | ✅ Endpoint accepts NLP query, returns prefilled form |

---

## Impact

- **Unblocks:** All prefill functionality in production
- **Frontend can now:** Call `/v1/actions/prepare` to get prefilled form data
- **User benefit:** Forms pre-populated with extracted entities from natural language

---

## Next Steps

- Phase 17: Readiness States (visual indicators for READY/NEEDS_INPUT/BLOCKED)
- Execute Plan 17-02 (visual indicators in SuggestedActions.tsx)
