# Plan 16.2-01 Summary — Unified Route Architecture Foundation

**Executed:** 2026-03-03
**Status:** ✅ COMPLETE

---

## Completed Tasks

### Task 1: Copy lens_matrix.json to Frontend ✅
- Copied `/apps/web/src/lib/lens_matrix.json` (763 lines)
- Contains 12 lenses with 81 mutate_actions
- Single source of truth for RBAC decisions

### Task 2: Create PermissionService ✅
- Created `/apps/web/src/services/permissions.ts` (~200 lines)
- Exports: `canPerformAction`, `getPermittedActions`, `getActionMetadata`, `requiresSignature`, `requiresConfirmation`, `getAllActions`, `getAllLenses`
- Reads from lens_matrix.json instead of hardcoded role arrays
- Supports lens name aliases (singular/plural)

Note: `usePermissions.ts` hook already existed and was updated to use the new service.

### Task 3+4: Create RouteShell.tsx with LensContent Delegation ✅
- Created `/apps/web/src/components/lens/RouteShell.tsx` (~350 lines)
- Features:
  - Feature flag gating (`isFragmentedRoutesEnabled()`)
  - Data fetching via react-query
  - Route-specific navigation callbacks (`onBack`, `onClose`, `onNavigate`, `onRefresh`)
  - Loading/Error/NotFound states
  - `LensRendererWithRouteCallbacks` internal component
  - Delegation to all 13 LensContent components

### Verification ✅
- TypeScript compiles clean (0 errors excluding pre-existing test file issues)

---

## Files Created/Modified

| File | Lines | Status |
|------|-------|--------|
| `apps/web/src/lib/lens_matrix.json` | 763 | Created (copied from docs) |
| `apps/web/src/services/permissions.ts` | ~200 | Created |
| `apps/web/src/components/lens/RouteShell.tsx` | ~350 | Created |
| `apps/web/src/hooks/usePermissions.ts` | 189 | Already existed, depends on new service |

---

## Architecture Achievement

```
┌─────────────────────────────────────────────────────────────┐
│  Route Page (~20 LOC)                                        │
│  <RouteShell entityType="fault" entityId={id} listRoute="/faults" />
└──────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  RouteShell (~350 LOC)                                       │
│  - Feature flag gating                                       │
│  - Data fetching (react-query)                               │
│  - Navigation callbacks                                      │
│  - Loading/Error/NotFound states                            │
└──────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  LensContent Components (~6,500 LOC total)                   │
│  - All UI logic, buttons, modals                             │
│  - All 81 actions wired                                      │
└──────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  PermissionService (~200 LOC)                                │
│  - Reads from lens_matrix.json                               │
│  - canPerformAction(role, actionId, lens)                    │
│  - Single source of truth for RBAC                           │
└──────────────────────────────────────────────────────────────┘
```

---

## Next Steps (Future Plans)

1. **Plan 16.2-02**: Migrate 12 fragmented route pages to use RouteShell
2. **Plan 16.2-03**: Update existing permission hooks to use PermissionService
3. **Plan 16.2-04**: Cleanup dead code and verify E2E tests

---

## Acceptance Criteria Status

| Requirement | Status |
|-------------|--------|
| ROUTE-ARCH-01: RouteShell handles feature flag gating | ✅ |
| ROUTE-ARCH-02: usePermissions reads from lens_matrix.json | ✅ |
| TypeScript compiles clean | ✅ |

---

## Rollback Strategy

If issues arise, delete:
- `apps/web/src/components/lens/RouteShell.tsx`
- `apps/web/src/services/permissions.ts`
- `apps/web/src/lib/lens_matrix.json`

The existing fragmented routes remain functional.
