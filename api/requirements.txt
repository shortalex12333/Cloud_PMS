# CelesteOS Extraction Service Dependencies
# For Render deployment

# FastAPI and web server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# HTTP and CORS
python-multipart==0.0.6

# Security
PyJWT==2.8.0
slowapi==0.1.9

# Logging and monitoring
python-json-logger==2.0.7

# OpenAI - GPT-4o-mini extraction + text-embedding-3-small
# REQUIRED for proper entity extraction and vector search
openai>=1.3.0

# Supabase - Database + match_documents() vector search
# Pin to specific version to avoid dependency conflicts
supabase==2.10.0

# Testing (optional, for local development)
pytest==7.4.3
pytest-asyncio==0.21.1
# Note: httpx version managed by supabase dependency
